import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  closeButton: {
    display: 'flex',
    alignItems: 'flex-end',
  },
  email: {
    paddingVertical: 10,
    paddingHorizontal: 20,
    backgroundColor: 'red',
  },
});
